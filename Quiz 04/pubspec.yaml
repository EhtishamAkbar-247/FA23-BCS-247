name: quiz_4_auth
description: A pro-level Supabase Auth app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Core Supabase dependency
  supabase_flutter: ^2.0.0
  # For pro-level animations
  flutter_animate: ^4.2.0
  # For professional typography
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true